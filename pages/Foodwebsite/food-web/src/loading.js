import Logo from './Images/Logo3.jpg';

const Loading = () => {
    return(
        <header className="loading-body">
            <img src={Logo} className="App-logo" alt="logo" />
        </header>
    );
}

export default Loading;